= simple_form_for(@cart_item, url: add_item_path) do|f|
  = f.input :quantity
  = f.input :item_id, as: :hidden, input_html: {calue: @item.id }
  = f.button :submit, 'カートに追加', claas: 'btn btn-primary'
